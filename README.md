# QR Code Generator

This Python project generates a **custom-styled QR code** with:

- A custom **color scheme**
- An embedded **center logo or icon**
- High error correction to preserve scan reliability

---

## Requirements

Install required packages using `pip`:

```bash
pip install qrcode[pil] pillow